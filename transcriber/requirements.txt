openai-whisper
pyannote.audio
# pyannote.audio 3.4.0 usa hf_hub_download(use_auth_token=...), eliminado en
# huggingface_hub 1.0. Hay que mantenerlo por debajo de esa versión.
huggingface_hub<1.0
pydub
# torch>=2.6 cambió el default de torch.load a weights_only=True, lo que rompe la carga
# del checkpoint de pyannote.audio 3.4.0 (WeightsUnpickler error: TorchVersion). Mantener
# torch<2.6 (usa weights_only=False por defecto) hasta subir pyannote a una versión compatible.
torch<2.6
torchaudio<2.6
python-dotenv
